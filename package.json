{
  "name": "clonethis",
  "version": "0.1.0",
  "description": "Clone one component off a live site 1:1. Measured subtree, used CSS, states, motion, a standalone snapshot, and a verify gate.",
  "type": "module",
  "bin": {
    "clonethis": "./bin/clonethis"
  },
  "scripts": {
    "clonethis": "bun src/cli.ts",
    "typecheck": "tsc --noEmit",
    "test": "bun test/e2e.ts"
  },
  "dependencies": {
    "node-html-parser": "^7.0.1",
    "playwright": "^1.48.0",
    "sharp": "^0.33.5"
  },
  "devDependencies": {
    "@types/bun": "latest",
    "typescript": "^5.6.0"
  },
  "trustedDependencies": [
    "sharp"
  ]
}
